# Setup Log - 2026-06-02

## Scope

Created a local Git repository for a public Taiwan social welfare resource finder, with crawler and freshness-check scaffolding for later GitHub/Vercel production deployment.

## Research Used

- Open Referral HSDS for organization, service, location, and metadata structure.
- Findhelp and government service finder patterns for location, category, audience, and eligibility filters.
- Inform USA standards for update verification and interim updates.
- Taiwan official examples: MOHW e寶箱, Tainan welfare map open data, Tainan welfare map, Taichung welfare resource network, Taipei welfare map.

## Current Data

- 5 allowlisted sources in `data/sources.json`.
- 24 generated resource cards in `data/resources.json`.
- Tainan open-data import currently uses the HTML JSON preview available from the official resource page. If a full direct JSON endpoint is confirmed later, switch the importer to that endpoint.
- Kevin-provided Downloads files were not found locally during setup. `data/extracted-urls.json` records zero extracted URLs. Put files in `source-docs/` and rerun `scripts/extract_source_urls.py`.

## Verification

- `python scripts/validate_data.py`: passed with 24 records and 5 sources.
- Python compile check passed for all scripts.
- Freshness check ran against 6 URLs. Taipei map reported a local certificate verification warning; source remains queued for review.
- Local preview served at `http://127.0.0.1:4174`.
- Browser check confirmed 24 cards load and search for `復康巴士` narrows to 1 record.
- Chrome desktop and mobile screenshots were inspected from `work/desktop-chrome.png` and `work/mobile-chrome-after.png`.
- Review-stage noindex is active in `index.html`, `robots.txt`, and `vercel.json`.

## Deployment Gate

Not yet pushed or deployed. Kevin approval is required before:

- creating a public GitHub repo;
- pushing this repository;
- connecting or creating a Vercel project;
- enabling GitHub Actions as a live recurring automation;
- producing a Production deployment;
- removing noindex or allowing search indexing.
